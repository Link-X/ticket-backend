<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ticket.core.mapper.UserMapper">

    <!-- 用户结果映射 -->
    <resultMap id="UserResultMap" type="com.ticket.core.domain.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 插入用户（id 由应用层生成，非数据库自增） -->
    <insert id="insert" parameterType="com.ticket.core.domain.entity.User">
        INSERT INTO `user` (id, username, phone, email, password_hash, status, create_time, update_time)
        VALUES (#{id}, #{username}, #{phone}, #{email}, #{passwordHash}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- 根据 ID 查询用户 -->
    <select id="selectById" resultMap="UserResultMap">
        SELECT id, username, phone, email, password_hash, status, create_time, update_time
        FROM `user`
        WHERE id = #{id}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT id, username, phone, email, password_hash, status, create_time, update_time
        FROM `user`
        WHERE username = #{username}
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" resultMap="UserResultMap">
        SELECT id, username, phone, email, password_hash, status, create_time, update_time
        FROM `user`
        WHERE phone = #{phone}
    </select>

</mapper>
